<h1>Articles</h1>
<% @articles.each do |a| %>
  <h2><%= a.title %></h2>
  <%= link_to "read article", article_path(:id => a.id) %>
  <%= link_to "delete article", article_path(:id => a.id), method: :delete %>
  <p></p>
<% end %>

<%= link_to "add a new article", new_article_path %>


